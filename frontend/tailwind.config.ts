// Tailwind CSS v4: configuration has moved to CSS (@theme in styles/globals.css).
// This file is kept for tooling compatibility but is no longer read by Tailwind.
// See: https://tailwindcss.com/docs/upgrade-guide
import type { Config } from 'tailwindcss';

const config: Config = {};

export default config;
